# FPV DRONE DETECTOR — מדריך מלא

> מערכת זיהוי רחפנים אקוסטי + ויזואלי | גרסה 3.0
> כתוב בעברית פשוטה — ללא ז'רגון

---

## תוכן עניינים

1. [מה המערכת עושה — זרימת הנתונים](#1-מה-המערכת-עושה)
2. [מושגי יסוד — הסבר פשוט](#2-מושגי-יסוד)
3. [התקנה מלאה](#3-התקנה-מלאה)
4. [הפעלה — כמה טרמינלים צריך](#4-הפעלה)
5. [פרוטוקול שטח — צ'קליסט](#5-פרוטוקול-שטח)
6. [קריאת המסך — כל שדה מוסבר](#6-קריאת-המסך)
7. [ניתוח CSV אחרי הבדיקה](#7-ניתוח-csv)
8. [טבלת תדרים לפי סוג רחפן](#8-טבלת-תדרים)
9. [בעיות נפוצות — 20+ false positives](#9-false-positives)
10. [הגבלות המערכת](#10-הגבלות)

---

## 1. מה המערכת עושה

### זרימת הנתונים — מהמיקרופון עד ההחלטה

```
                    ┌─────────────────────────────────────────────────────────┐
                    │                  ACOUSTIC PIPELINE                      │
                    │                                                          │
 מיקרופון ─────────►  FFT  ──► BPF Detection ──► SNR Check ──► Harmonics ──►─┐│
 (1024 דגימות/פריים) │                                                         ││
                    │  ↑ מחשב תדר שיא    ↑ מסנן רעש    ↑ מוודא כפולות        ││
                    └─────────────────────────────────────────────────────────┘│
                                                                               │ EMA
                    ┌─────────────────────────────────────────────────────────┐│ smoothing
                    │                   VISUAL PIPELINE                       ││
                    │                                                          ││
 מצלמה ────────────►  YOLOv8  ──► Drone Class ──► Confidence ────────────────►┤│
 (פריים × 30fps)    │                                                          ││
                    │  ↑ רשת עצבית      ↑ סינון קלאס   ↑ 0.0–1.0             ││
                    └─────────────────────────────────────────────────────────┘│
                                                                               │
                                                                               ▼
                                                               combined_threat()
                                                                               │
                                    ┌──────────────────────────────────────────┤
                                    │                                          │
                            ┌───────▼──────┐  ┌──────────────┐  ┌────────────▼────────┐  ┌────────┐
                            │  CONFIRMED   │  │ ACOUSTIC     │  │ VISUAL ONLY         │  │ CLEAR  │
                            │  אקוסטי+    │  │ ONLY         │  │ ראה, לא שמע         │  │ שקט   │
                            │  ויזואלי    │  │ שמע, לא ראה  │  │                     │  │        │
                            └──────────────┘  └──────────────┘  └─────────────────────┘  └────────┘
```

### משמעות כל מצב — מה לעשות

| מצב | צבע | משמעות | פעולה מומלצת |
|-----|-----|--------|--------------|
| **CONFIRMED** | 🔴 אדום מהבהב | שתי שכבות זיהוי בו-זמנית — ביטחון גבוה מאוד | פעולה מיידית נדרשת |
| **ACOUSTIC ONLY** | 🟡 צהוב | שמענו אבל לא ראינו — מחוץ לשדה ראייה, לילה, ערפל, גב המצלמה | חפשו ויזואלית, היו דרוכים |
| **VISUAL ONLY** | 🔵 כחול | ראינו אבל לא שמענו — חשמלי שקט, גבוה מאוד, רעש רקע חזק | אשרו ויזואלית, הכינו תגובה |
| **CLEAR** | 🟢 ירוק | אף שכבה לא עברה סף — ניטור רגיל | המשיכו לנטר |

---

## 2. מושגי יסוד

### BPF — Blade Pass Frequency (תדר מעבר הלהב)

**המשל:** כל מאוורר בבית מרעיש בתדר אחר. מאוורר תקרה גדול — רעש עמוק ונמוך. מאוורר קטן ליד המחשב — שריקה דקה וגבוהה. זה בגלל שמספר הסיבובים שונה.

**הסבר טכני:** כל פעם שלהב חותך את האוויר הוא יוצר פולס לחץ. אם יש 2 להבים ומנוע ב-25,000 RPM:

```
BPF = RPM ÷ 60 × מספר להבים
BPF = 25,000 ÷ 60 × 2 = 833 Hz
```

**מה המספר אומר בפועל:**
- `f₀ = 500 Hz` → רחפן בינוני-גדול (FPV 7" או Lancet)
- `f₀ = 850 Hz` → FPV 5" טיפוסי — הנפוץ ביותר בסכסוכים
- `f₀ = 183 Hz` → DJI Mavic — ריגול/ספוטינג
- `f₀ = 240 Hz` → Shahed-136 — מנוע בנזין

**ערך חריג:** f₀ קבוע לחלוטין לאורך זמן (**לא** משתנה) = כנראה לא רחפן אלא מכשיר חשמלי (ראו סעיף 9).

---

### FFT — Fast Fourier Transform (ניתוח תדרים)

**המשל:** אתם עומדים ליד תזמורת ושומעים כל כלי ביחד — FFT הוא כמו אוזן מוזיקלית שיכולה לפרק את כל הצלילים ולהגיד: "יש כינור ב-440Hz, חצוצרה ב-880Hz, תוף ב-60Hz."

**הסבר טכני:** לוקחים 1,024 דגימות (פריים של ~64ms), מחילים חלון Hanning למניעת דליפה, מריצים `rfft` — מקבלים גרף של עוצמה לפי תדר.

**מה המספר אומר בפועל:** הצומת הגבוה ביותר בגרף (בין 80–1,600Hz) הוא ה-f₀ המועמד. אם אין צומת בולט — רק רעש לבן.

---

### SNR — Signal-to-Noise Ratio (יחס אות לרעש)

**המשל:** אתם בבר רועש ומישהו לוחש לכם. כשהמוזיקה שותקת — שומעים בקלות (SNR גבוה). כשהמוזיקה עצמה צועקת — כלום (SNR נמוך).

**הסבר טכני:**
```
SNR = עוצמת הצומת ב-f₀ ÷ חציון עוצמת כל התדרים בטווח
```

**מה המספר אומר בפועל:**

| SNR | משמעות | פעולה |
|-----|--------|-------|
| `< 3×` | רעש רקע בלבד — מתעלמים | לא מעבדים את הפריים |
| `3–8×` | אות חלש — רחפן רחוק או רעש גבוה | זיהוי אפשרי, ביטחון נמוך |
| `8–15×` | אות טוב — טווח בינוני | זיהוי סביר |
| `> 15×` | אות חזק — רחפן קרוב | זיהוי בטוח, NEAR range |

---

### הרמוניות (Harmonics)

**המשל:** כשגיטריסט מנגן Do, אתם שומעים לא רק את ה-Do אלא גם כפולות שלו — Do גבוה יותר, Sol, Do גבוה עוד יותר. זה הצליל העשיר של הגיטרה לעומת צפצוף דיגיטלי "יבש".

**הסבר טכני:** רוטור שמייצר f₀=847Hz מייצר גם:
```
847  Hz  ← f₀ (הבסיסי)
1694 Hz  ← f₀ × 2
2541 Hz  ← f₀ × 3
3388 Hz  ← f₀ × 4
```

המערכת בודקת שגם הכפולות קיימות. אם רק f₀ קיים ללא כפולות — כנראה הפרעה אלקטרונית, לא רחפן.

**מה המספר אומר בפועל:**
- `harmonics ≥ 3` → ביטחון גבוה
- `harmonics = 2` → ביטחון בינוני (h2 ו-h3 בלבד)
- `harmonics < 2` → נדחה — לא עומד בקריטריון

---

### EMA — Exponential Moving Average (ממוצע נע אקספוננציאלי)

**המשל:** אתם מנסים להחליט אם חבר שלכם עצוב. לא מסתמכים על הבעת פנים בשנייה אחת — מסתכלים על ממוצע ההתנהגות של יום שלם, עם דגש על השעה האחרונה.

**הסבר טכני:**
```
conf_חדש = 0.25 × conf_פריים + 0.75 × conf_קודם
```

פירוש: ביטחון הפריים הנוכחי תורם 25%, היסטוריה תורמת 75%. קפיצות בודדות מוחלקות.

**מה המספר אומר בפועל:**
- `EMA > 35%` לשני פריימים רצופים → אזעקה מופעלת (`ALARM_FRAMES = 2`)
- ביטחון גולמי של 80% בפריים יחיד **לא** מפעיל אזעקה — חייב להחזיק

---

### Doppler — כיוון ומרחק

**המשל:** אמבולנס ניגש אליכם — הסירנה עולה ומתחדדת. עובר ומתרחק — הסירנה יורדת ומתמעמת. זה אפקט Doppler: מקור שמתקרב "מדחס" גלים, מתרחק "מאריך" אותם.

**הסבר טכני:** המערכת שומרת 10 פריימים אחרונים של f₀ ומחשבת שינוי:
```
מחצית ראשונה → מחצית שנייה
אם הפרש > +15 Hz/sec → APPROACHING
אם הפרש < -15 Hz/sec → RECEDING
אחרת → STABLE
```

**מה המספר אומר בפועל:**
- `+45 Hz/sec` → רחפן מתקרב מהר (FPV בשיוט ישיר)
- `+12 Hz/sec` → מתקרב לאט (מרחף ומקרב)
- `0 Hz/sec` → מרחף במקום (ספוטינג/השקה)
- `-30 Hz/sec` → מתרחק — לאחר מעבר

---

### rms_gate — שער הדממה

**המשל:** אתם מדברים בשקשוק ורוצים להקליט רק כשמישהו צועק. מגדירים סף — מתחתיו המיקרופון "ישן", מעליו "מתעורר". זה ה-rms_gate.

**הסבר טכני:** לפני כל FFT, מחשבים `RMS = √(mean(samples²))`. אם מתחת לסף — מדלגים על הפריים הזה לחלוטין (חיסכון משאבים + מניעת noise).

**מה המספר אומר בפועל:**
- `rms_gate` נמוך מדי → כל רעש סביבה נכנס → false positives
- `rms_gate` גבוה מדי → גם הרחפן נחסם → false negatives
- **כיול נכון:** הרץ `--calibrate` 60 שניות בסביבה נקייה

---

## 3. התקנה מלאה

### דרישות מינימום

| רכיב | מינימום | מומלץ |
|------|---------|-------|
| Python | 3.11 | 3.12 |
| RAM | 4 GB | 8 GB |
| מיקרופון | מובנה | USB cardioid |
| מצלמה | — (אופציונלי) | 720p USB |
| OS | Windows 10 / Ubuntu 20 / macOS 12 | Ubuntu 22 |

---

### שלב 1 — שכפול הקוד

```bash
git clone https://github.com/itay313122-max/roi-bot.git
cd roi-bot
```

### שלב 2 — התקנת ספריות

#### Linux / macOS

```bash
# portaudio — ספריית C שעליה pyaudio רץ (חובה קודם!)
sudo apt-get install -y portaudio19-dev   # Ubuntu/Debian
# או:
brew install portaudio                    # macOS

# כל הספריות בבת אחת
pip install pyaudio numpy ultralytics opencv-python rich
```

#### Windows

```bash
# שיטה רגילה (ייתכן שתיכשל עם pyaudio)
pip install numpy ultralytics opencv-python rich

# אם pip install pyaudio נכשל על Windows — פתרון מוכח:
pip install pipwin
pipwin install pyaudio

# אם גם pipwin נכשל — התקינו wheel ידנית:
# הורידו מ-https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# לפי גרסת Python שלכם (cp311 = Python 3.11)
pip install PyAudio-0.2.14-cp311-cp311-win_amd64.whl
```

---

### מה כל ספרייה עושה

| ספרייה | תפקיד | בלעדיה... |
|--------|--------|-----------|
| **pyaudio** | מאזינה למיקרופון בזמן אמת — לוקחת 1,024 דגימות כל פריים | האקוסטיקה עוברת לסימולציה |
| **numpy** | מריצה FFT, חישובי RMS, מערכים של מיליוני מספרים בשניה | FFT יהיה 100× איטי יותר |
| **ultralytics** | YOLOv8 — הרשת העצבית שמזהה רחפן בתמונה | אין שכבה ויזואלית |
| **opencv-python** | פותחת webcam, קוראת פריימים, מצייר תיבות על המסגרת | אין תמיכת מצלמה |
| **rich** | מציג ממשק טרמינל צבעוני עם עדכונים בזמן אמת | ממשק שחור-לבן בסיסי |

---

### שלב 3 — אימות התקנה

```bash
# בדיקה שהכל מותקן
python3 -c "
import numpy, ultralytics, cv2, rich
print('numpy:', numpy.__version__)
print('ultralytics:', ultralytics.__version__)
print('cv2:', cv2.__version__)
print('rich: OK')
"

# בדיקת pyaudio + מיקרופון
python3 -c "
import pyaudio
p = pyaudio.PyAudio()
found = False
for i in range(p.get_device_count()):
    d = p.get_device_info_by_index(i)
    if d['maxInputChannels'] > 0:
        print(f'מיקרופון [{i}]: {d[\"name\"]}')
        found = True
if not found:
    print('לא נמצא מיקרופון — יעבוד בסימולציה')
p.terminate()
"
```

---

## 4. הפעלה

### אפשרות א' — 3 טרמינלים (הדרך המלאה)

כל תהליך רץ בנפרד כדי שאפשר לראות את הפלט שלו ולסגור כל אחד בנפרד:

```
┌─────────────────────────────────────────────────────────────┐
│  טרמינל 1 — הלב של המערכת                                   │
│                                                              │
│  python drone_detector.py                                    │
│                                                              │
│  מריץ את כל הניתוח, שומר את המצב, מגיש HTTP על פורט 8765   │
│  חייב לרוץ כל עוד רוצים לזהות                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  טרמינל 2 — הממשק בשטח                                      │
│                                                              │
│  python ui_field.py                                          │
│                                                              │
│  שואל את טרמינל 1 כל 500ms ומציג הכל בצורה נוחה            │
│  כאן לוחצים C/L/R/Q                                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  טרמינל 3 — הקלטת נתונים (אופציונלי)                        │
│                                                              │
│  python test_logger.py --no-launch                           │
│                                                              │
│  מקליט CSV בשקט — שימושי כשממשק השטח כבר פתוח              │
│  מחכה עד שתפסיקו עם Ctrl+C                                  │
└─────────────────────────────────────────────────────────────┘
```

### אפשרות ב' — 2 טרמינלים (מהיר יותר)

```bash
# טרמינל 1 — detector + logger ביחד
python drone_detector.py &         # מפעיל ברקע
python test_logger.py --no-launch  # מתחבר מיד, מקליט CSV

# טרמינל 2 — ממשק שטח
python ui_field.py
```

**מתי להשתמש:** כשיש רק מסך אחד ורוצים לראות את המסך הצבעוני + להקליט CSV.

### אפשרות ג' — ממשק דפדפן (עמדת פיקוד)

```bash
# הפעילו detector בלבד
python drone_detector.py

# פתחו בדפדפן (על אותו מחשב)
http://localhost:8765

# פתחו מרחוק (אם אותו רשת)
http://<IP של המחשב>:8765
```

**מתי להשתמש:** תצוגה על מסך גדול, ריבוי משתמשים, מפקד שרואה מרחוק.

---

## 5. פרוטוקול שטח

### לפני היציאה מהבסיס

```bash
# □ 1. בדיקת מיקרופון
python3 -c "import pyaudio; p=pyaudio.PyAudio(); [print(p.get_device_info_by_index(i)['name']) for i in range(p.get_device_count()) if p.get_device_info_by_index(i)['maxInputChannels']>0]; p.terminate()"

# □ 2. בדיקת YOLOv8
python3 -c "from ultralytics import YOLO; YOLO('yolov8n.pt'); print('YOLOv8 OK')"

# □ 3. בדיקת ריצה מהירה (10 שניות)
timeout 10 python drone_detector.py 2>&1 | tail -5

# □ 4. מנעו שינה של המחשב
# Linux:
sudo systemctl mask sleep.target suspend.target hibernate.target
# Windows: הגדירו Power Plan → Never sleep

# □ 5. הכינו תיקיית פלט
mkdir -p ~/field_sessions/$(date +%Y%m%d)
```

---

### בנקודת הצבה

```bash
# □ 1. הצבת מיקרופון
#    - גובה: 1–1.5 מטר מהקרקע
#    - כיוון: פתוח ל-360 מעלות (לא ליד קיר)
#    - מרחק מרעש: 5+ מטר ממחוללים/רכבים
#    - רוח: Foam windscreen חובה אם > 15 קמ"ש

# □ 2. קליברציה בסביבה החדשה (30 שניות ללא רחפנים)
python drone_detector.py --calibrate --cal-duration 30

# □ 3. עדכנו rms_gate לפי הפלט
# ב-drone_detector.py, שורה ~61:
# RMS_GATE = 0.000XXX   ← הערך שקיבלתם

# □ 4. הפעלה
python drone_detector.py &    # ברקע

# □ 5. ממשק שטח
python ui_field.py

# □ 6. התחילו הקלטה — לחצו L בממשק
#    או בטרמינל נפרד:
python test_logger.py --no-launch --output ~/field_sessions/$(date +%Y%m%d)/session_$(date +%H%M).csv
```

---

### אחרי הבדיקה

```bash
# □ 1. עצרו הקלטה — לחצו L שוב (או Ctrl+C ב-test_logger)

# □ 2. סגרו ממשק — לחצו Q

# □ 3. עצרו detector
pkill -f drone_detector.py

# □ 4. העתיקו נתונים
cp ~/field_sessions/$(date +%Y%m%d)/*.csv /media/usb/   # לUSB
# או:
scp ~/field_sessions/$(date +%Y%m%d)/*.csv user@server:/data/

# □ 5. הפעילו שוב שינה
sudo systemctl unmask sleep.target suspend.target hibernate.target

# □ 6. ניתוח — ראו סעיף 7
```

---

## 6. קריאת המסך

### מסך מלא עם שדות מסומנים

```
┌────────────────────────────────────────────────────────┬──────────────────┐
│ [A]FPV DETECT v2.0 — FIELD MODE   [B]● log (423 rows)  │ [C]QUICK GUIDE   │
│                                   UPTIME 00:07:43       │                  │
├─────────────────────────┬──────────────────────────────┤  f₀=[P] תדר      │
│ [D]ACOUSTIC             │ [E]VISUAL (YOLOv8)           │  הבסיסי של הרוטור│
│ [F]████████░░  73%      │ [G]█████░░░░░  50%           │                  │
│ [H]f₀   847 Hz          │ [I]Model drone-detection     │  RPM = f₀×60÷2   │
│ [J]RPM  ~25,410         │ [K]FPS   14.2                │                  │
│ [L]Type FPV 5" Racing   │ [M]Source LIVE CAMERA        │  SNR = עוצמה÷רעש │
│    SNR  8.3×            │   Alert  no                  │                  │
├─────────────────────────┴──────────────────────────────┤  NEAR = SNR>15   │
│                                                        │  MED  = SNR 8-15 │
│  [N]        ⚠  ACOUSTIC ONLY                           │  FAR  = SNR<8    │
│                                                        │                  │
│  [O]Direction : ↑  APPROACHING   (+23.4 Hz/sec)        │  ↑APPROACHING    │
│     Est. Range: MEDIUM (50–200m)                       │  = f₀ עולה      │
│                                                        │                  │
├────────────────────────────────────────────────────────┤  ↓RECEDING       │
│ noise floor: 0.000312  rms_gate: 0.000579  frames:1847 │  = f₀ יורד      │
├─ DETECTION LOG ────────────────────────────────────────┤                  │
│  TIME     TYPE          f₀      SNR    THREAT   DIR    │  —STABLE         │
│  08:42:31 FPV 5" Racing 847Hz   8.3×  CONFIRM  APPR   │  = f₀ יציב      │
│  08:41:15 FPV 5" Racing 831Hz   6.1×  ACOUSTIC STABLE  │                  │
│  08:39:02 DJI Mavic     183Hz   4.2×  VISUAL   RECED  │                  │
├────────────────────────────────────────────────────────┤                  │
│  [P1]C Calibrate  [P2]L Log  [P3]R Reset  [P4]Q Quit   │                  │
└────────────────────────────────────────────────────────┴──────────────────┘
```

### טבלת כל שדה — A עד P

| אות | שדה | מה הוא | טווח נורמלי | ערך חריג — מה פירושו |
|-----|-----|--------|-------------|---------------------|
| **A** | כותרת + גרסה | זיהוי המערכת | — | — |
| **B** | מצב לוגינג + זמן ריצה | ● אדום = מקליט CSV; ○ = לא מקליט | uptime עולה בהתמדה | uptime קפץ = detector הופעל מחדש |
| **C** | פאנל QUICK GUIDE | תזכורת מושגים — לא משתנה | — | — |
| **D** | כותרת פאנל אקוסטי | גבול השכבה הראשונה | — | אדום = אזעקה פעילה |
| **E** | כותרת פאנל ויזואלי | גבול השכבה השנייה | — | אדום = alert פעיל |
| **F** | בר ביטחון אקוסטי | ביטחון EMA-מוחלק | 0–30% כשאין רחפן | מעל 35% = מתקרב לאזעקה |
| **G** | בר ביטחון ויזואלי | ביטחון YOLOv8 | 0% בלי מצלמה/רחפן | מעל 60% = VISUAL alert |
| **H** | f₀ (Hz) | תדר הרוטור שנמצא | 0 כשאין זיהוי | ראו טבלת תדרים סעיף 8 |
| **I** | Model | מודל YOLOv8 שנטען | drone-specific (HF) עדיף | COCO = מודל כללי, פחות מדויק |
| **J** | RPM משוער | f₀ × 60 ÷ 2 להבים | 0 כשאין זיהוי | RPM גבוה מאוד (>45k) = FPV 3" |
| **K** | FPS מצלמה | כמה פריימים/שנייה מעובדים | 15–30 FPS | < 5 FPS = עומס CPU גבוה |
| **L** | סוג רחפן משוער | פרופיל שזוהה מה-DRONE_DATABASE | — עם אין זיהוי | "—" עם f₀>0 = פרופיל לא הוגדר |
| **M** | מקור וידאו | LIVE CAMERA / SIMULATION | LIVE עם מצלמה | SIMULATION = אין מצלמה (נורמלי בשדה ללא webcam) |
| **N** | Combined Threat | שילוב שתי השכבות | CLEAR רוב הזמן | CONFIRMED = שתיהן פעילות |
| **O** | Direction + Rate | כיוון Doppler + מהירות שינוי | STABLE ≈ 0 Hz/sec | > ±50 Hz/sec = רחפן מהיר מאוד |
| **P** | מקשי פעולה | קיצורי מקלדת | — | ראו טבלה מטה |

### מקשי פעולה — P

| מקש | פעולה | מה קורה |
|-----|-------|---------|
| **C** | Calibrate | מפעיל 30s קליברציה — פאנל האיום מציג progress bar |
| **L** | Log CSV | מפעיל/עוצר הקלטה. שם קובץ: `field_test_YYYYMMDD_HHMMSS.csv` |
| **R** | Reset | מאפס frames, detections, ערכי קליברציה — לא מפסיק הקלטה |
| **Q** | Quit | יציאה נקייה — שומר ומסגר CSV לפני כיבוי |

---

## 7. ניתוח CSV

### עמודות הקובץ

| עמודה | פורמט | הסבר |
|-------|--------|-------|
| `timestamp` | `2026-05-29T08:42:31.456` | זמן מדויק עד מילישניות |
| `acoustic_confidence` | `0.0–100.0` | ביטחון EMA אחרי החלקה |
| `visual_confidence` | `0.0–100.0` | ביטחון YOLOv8 |
| `combined_threat` | מחרוזת | CONFIRMED / ACOUSTIC ONLY / VISUAL ONLY / CLEAR |
| `f0_hz` | שלם | התדר שנמצא. 0 = אין זיהוי |
| `rpm_estimate` | שלם | RPM משוער |
| `drone_type` | מחרוזת | פרופיל שזוהה. `—` = אין |
| `direction` | מחרוזת | APPROACHING / RECEDING / STABLE |
| `distance_phase` | מחרוזת | INBOUND / OUTBOUND / HOVER/OVERHEAD / N/A |

---

### קוד Python לניתוח מלא

```python
import pandas as pd
import matplotlib.pyplot as plt
import matplotlib.patches as mpatches

df = pd.read_csv("field_test.csv", parse_dates=["timestamp"])
df = df.set_index("timestamp")

# ── 1. סיכום מהיר ────────────────────────────────────────────────────
print("=" * 50)
print("סיכום סשן")
print("=" * 50)
print(f"זמן ריצה:     {df.index.min()} → {df.index.max()}")
print(f"משך:          {df.index.max() - df.index.min()}")
print(f"סה\"כ שורות:   {len(df)}")
print(f"\nפירוט לפי סוג:")
print(df["combined_threat"].value_counts().to_string())

confirmed = df[df["combined_threat"] == "CONFIRMED"]
if not confirmed.empty:
    print(f"\nאירועי CONFIRMED: {len(confirmed)}")
    print(f"  f₀ ממוצע:     {confirmed['f0_hz'].mean():.0f} Hz")
    print(f"  RPM ממוצע:    {confirmed['rpm_estimate'].mean():,.0f}")
    print(f"  סוג נפוץ:     {confirmed['drone_type'].mode()[0]}")

# זיהוי false positives: f0 קבוע + תמיד STABLE
stable_events = df[(df["direction"] == "STABLE") & (df["f0_hz"] > 0)]
if not stable_events.empty:
    f0_std = stable_events["f0_hz"].std()
    if f0_std < 5:
        print(f"\n⚠ ייתכן false positive: f₀ קבוע מאוד (std={f0_std:.1f} Hz)")
        print("   בדקו אם יש HVAC / מאוורר / גנרטור בסביבה")

# ── 2. גרף confidence לאורך זמן עם סימון אירועים ────────────────────
fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(15, 7), sharex=True)

# גרף עליון — confidence
df["acoustic_confidence"].plot(ax=ax1, color="limegreen",
                                label="אקוסטי", linewidth=1.2)
df["visual_confidence"].plot(ax=ax1, color="dodgerblue",
                              label="ויזואלי", linewidth=1.2, alpha=0.8)
ax1.axhline(35, color="orange", linestyle="--", linewidth=1, label="סף אזעקה (35%)")
ax1.axhline(60, color="red",    linestyle="--", linewidth=1, label="סף ויזואלי (60%)")

# סימון אירועי CONFIRMED באדום
for ts, row in confirmed.iterrows():
    ax1.axvline(ts, color="red", alpha=0.3, linewidth=2)

ax1.set_ylabel("ביטחון (%)")
ax1.set_title("ציר זמן זיהוי — Confidence")
ax1.legend(loc="upper right")
ax1.set_ylim(0, 105)
ax1.grid(True, alpha=0.3)

# גרף תחתון — f₀ Doppler
f0_data = df[df["f0_hz"] > 0]["f0_hz"]
f0_data.plot(ax=ax2, color="purple", marker=".", markersize=3,
             linewidth=0.8, label="f₀ (Hz)")

# צביעת רקע לפי כיוון
for ts, row in df[df["f0_hz"] > 0].iterrows():
    color = {"APPROACHING": "#ffcccc", "RECEDING": "#ccccff",
             "STABLE": "#ccffcc"}.get(row["direction"], "white")
    ax2.axvline(ts, color=color, alpha=0.4, linewidth=3)

ax2.set_ylabel("f₀ (Hz)")
ax2.set_xlabel("זמן")
ax2.set_title("תדר בסיסי — שינוי Doppler (אדום=מתקרב / כחול=מתרחק / ירוק=יציב)")
ax2.legend()
ax2.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig("analysis.png", dpi=150)
plt.show()
print("\nגרף נשמר: analysis.png")
```

---

### דוגמת CSV טובה — Flyover ברור

```
timestamp,           acoustic_confidence, f0_hz, combined_threat, direction
2026-05-29T08:42:31, 12.3,                0,     CLEAR,           STABLE
2026-05-29T08:42:32, 31.4,               734,    ACOUSTIC ONLY,   APPROACHING
2026-05-29T08:42:33, 78.1,               847,    ACOUSTIC ONLY,   APPROACHING  ← הסף חצוי
2026-05-29T08:42:34, 91.2,               923,    CONFIRMED,       APPROACHING  ← גם ויזואלי
2026-05-29T08:42:35, 88.6,               956,    CONFIRMED,       STABLE       ← מעל הראש
2026-05-29T08:42:36, 71.3,               881,    CONFIRMED,       RECEDING     ← עובר
2026-05-29T08:42:37, 43.8,               812,    ACOUSTIC ONLY,   RECEDING     ← מתרחק
2026-05-29T08:42:38, 17.2,                 0,    CLEAR,           STABLE       ← נעלם
```

**איך לקרוא:** f₀ עולה (734→923→956) = Doppler APPROACHING, מגיע לשיא מעל הראש (STABLE), יורד (881→812→0) = מתרחק. זו תוצאה **אמינה**.

---

### דוגמת CSV רעה — False Positive (HVAC/מזגן)

```
timestamp,           acoustic_confidence, f0_hz, combined_threat, direction
2026-05-29T08:51:00, 38.1,               312,    ACOUSTIC ONLY,   STABLE
2026-05-29T08:51:01, 41.3,               312,    ACOUSTIC ONLY,   STABLE    ← f₀ קבוע!
2026-05-29T08:51:02, 39.7,               312,    ACOUSTIC ONLY,   STABLE
2026-05-29T08:51:03, 42.1,               312,    ACOUSTIC ONLY,   STABLE    ← אף פעם לא זז
2026-05-29T08:51:04, 40.8,               312,    ACOUSTIC ONLY,   STABLE
```

**הסימנים המזהים:**
- `f0_hz = 312` בדיוק בכל שורה — **לא משתנה אף הרץ**
- `direction = STABLE` לנצח — רחפן תמיד זז קצת
- אין `CONFIRMED` — אין ויזואלי תואם

**תיקון:** `SCAN_MIN_HZ = 350` כדי לדלג על תחום ה-HVAC, או `RMS_GATE × 2`.

---

## 8. טבלת תדרים

### גרף חפיפות תדרים

```
Hz:  100   200   300   400   500   600   700   800   900  1000  1200  1500
      |     |     |     |     |     |     |     |     |     |     |     |
      ↓     ↓     ↓     ↓     ↓     ↓     ↓     ↓     ↓     ↓     ↓     ↓

DJI Matrice:  [===]
Shahed-136:         [====]
DJI Mavic:          [=====]
Lancet:                    [=========]
FPV 7":                          [=========]
FPV 5":                                    [================]
FPV 3":                                          [===================]

אזורי חפיפה:
150–167 Hz → Mavic + Matrice    (נדיר, Matrice גדול מאוד)
200–217 Hz → Mavic + Shahed     (חשוב! ראו הערה מטה)
267–283 Hz → Shahed + Lancet
400–500 Hz → Lancet + FPV 7"
667–733 Hz → FPV 7" + FPV 5"
833–1200 Hz → FPV 5" + FPV 3"
```

> **הערה Shahed vs Mavic:** שניהם 200–283Hz. המבדל: Shahed מנוע בנזין = תנודות לא-עקביות ב-f₀ (±15Hz בין פריימים). Mavic חשמלי = f₀ יציב מאוד. בדקו את שונות f₀.

### טבלה מלאה — כל פרופיל

| סוג רחפן | BPF (Hz) | RPM | SNR מינימום | רמת איום | איך נשמע | מאפיין ייחודי |
|----------|----------|-----|------------|----------|----------|--------------|
| **FPV 5" Racing (Kamikaze)** | 667–1200 | 20k–36k | 6.0× | 🔴 HIGH | שריקה חדה, "וויז'", כמו נסירה מהירה | נפוץ ביותר בסכסוכים |
| **FPV 3" Micro (Suicide)** | 833–1500 | 25k–45k | 5.0× | 🔴 HIGH | שריקה דקה ועדינה, כמעט כמו יתוש | קטן, קשה לראות, זול |
| **FPV 7" Long Range** | 400–733 | 12k–22k | 5.0× | 🔴 HIGH | "ווווו" עמוק-בינוני, כמו מופד | מטווחים ארוכים, fiber-optic |
| **DJI Mavic (Recon/ISR)** | 150–217 | 4.5k–6.5k | 4.0× | 🟡 MEDIUM | זמזום נמוך, כמו דבורה גדולה | ריגול, ספוטינג ארטילריה |
| **DJI Matrice (Heavy Lift)** | 100–167 | 3k–5k | 3.0× | 🟡 MEDIUM | רעש עמוק ועבה, כמו מנוע קרוב | הטענת עומסים, רימונים |
| **Shahed-136 / Geran-2** | 200–283 | 6k–8.5k | 3.0× | 🔴 CRITICAL | טחטח לא-עקבי, כמו קטנוע ישן | מנוע בנזין — f₀ תנודתי |
| **Lancet Loitering Munition** | 267–500 | 8k–15k | 4.0× | 🔴 HIGH | שריקה בינונית-נמוכה, יחסית שקט | חשמלי, נגד שריון |

---

## 9. False Positives

### טבלת 20+ מקורות רעש ופתרונות

| מקור רעש | תדר אופייני | סימן מזהה | תיקון |
|----------|-------------|-----------|-------|
| **מזגן / HVAC** | 100–300 Hz | f₀ קבוע לחלוטין, STABLE לנצח | `SCAN_MIN_HZ = 350` |
| **מאוורר מחשב** | 50–200 Hz | קבוע, נמוך, מגביר בחום | `SCAN_MIN_HZ = 220` |
| **גנרטור דיזל** | 50/100/150 Hz | 50Hz בדיוק + הרמוניות | `SCAN_MIN_HZ = 180` |
| **טרקטור / טנק** | 80–300 Hz | f₀ עולה ויורד לפי דריכת גז | `ALARM_FRAMES = 5`, `SNR_MIN = 5` |
| **מטוס גבוה** | 200–600 Hz | עובר לאט (3–7 דקות), f₀ יציב | אין ויזואלי תואם |
| **מסוק** | 80–200 Hz | הרמוניות חזקות, f₀ יציב-ים | בדקו ויזואלי — מסוק נראה בקלות |
| **אופנוע** | 400–1200 Hz | f₀ משתנה עם ה-RPM, לא יציב | APPROACHING ואז RECEDING מהר |
| **מכונית עוברת** | 100–800 Hz | חולף (2–5 שניות), ברודבנד | `ALARM_FRAMES = 4` |
| **עורב / ציפור** | 500–2000 Hz | לא הרמוני, לא עקבי | הרמוניות לא יתאמו — נדחה |
| **רוח** | 0–500 Hz, אקראי | confidence מתנדנד, f₀ קופץ | foam windscreen, `RMS_GATE × 3` |
| **קידוח חשמלי** | 600–1200 Hz | f₀ ב-RPM קבוע, STABLE | `ALARM_FRAMES = 5` |
| **ילדים צועקים** | 800–3000 Hz | לא עקבי, אין הרמוניות | נדחה בשלב harmonic check |
| **כלב נובח** | 100–600 Hz | burst קצר, לא מתמשך | EMA מחליק — לא מגיע לסף |
| **אמבולנס / ניידת** | 600–1200 Hz | סירנה — f₀ עולה ויורד מחזורי | מדפוס לא הרמוני — נדחה |
| **ברז / מים** | 100–1000 Hz | ברודבנד רציף, אין פיק | SNR נמוך — נדחה |
| **מייבש כביסה** | 200–400 Hz | מחזורי, STABLE, בתוך בנין | `rms_gate` גבוה מדי רחוק |
| **ספינה / מנוע ים** | 80–200 Hz | f₀ יציב מאוד, רחוק | `SCAN_MIN_HZ = 220` בסביבת ים |
| **רכבת** | 100–500 Hz | עובר (30–90 שניות), Doppler חד | APPROACHING→RECEDING בקצב גבוה |
| **כלי עבודה (מסור)** | 800–1600 Hz | f₀ ב-RPM קבוע של המנוע | `ALARM_FRAMES = 6`, בדיקת ויזואלי |
| **אנטנה/ממסר RF** | — | לא אקוסטי — אבל יכול להפריע למיק USB | החליפו כבל USB, שנו פורט |
| **מכשיר Walkie-talkie** | — | הפרעה אלקטרומגנטית — לא אקוסטית | הרחיקו מהמיקרופון |

### איך לזהות false positive בשניות

```
שאלות לאבחון מהיר:

1. f₀ קבוע לחלוטין (std < 3 Hz)?
   כן → מקור קבוע (HVAC/גנרטור) — לא רחפן

2. direction = STABLE לאורך 30+ שניות?
   כן → מקור עומד — לא רחפן (רחפן תמיד זז קצת)

3. יש ויזואלי תואם? (combined_threat = CONFIRMED)
   לא → ביטחון נמוך יותר ב-ACOUSTIC ONLY

4. f₀ תואם לפרופיל בסיס הנתונים?
   לא → drone_type = "—" — ייתכן מקור לא מוכר

5. הזיהוי חד פעמי (< 2 שניות)?
   כן → רחפן תמיד נשמע לפחות 4–6 שניות — כנראה false positive
```

---

## 10. הגבלות

### טווח מקסימלי לפי סוג ותנאים

| סוג רחפן | שקט מוחלט | עיר רגילה | רעש חזק (> 70 dB) |
|----------|-----------|-----------|-------------------|
| **FPV 5" Racing** | 200–350 מטר | 100–180 מטר | 30–70 מטר |
| **FPV 3" Micro** | 100–200 מטר | 50–100 מטר | 20–50 מטר |
| **Shahed-136** | 400–600 מטר | 200–350 מטר | 80–150 מטר |
| **DJI Mavic** | 80–140 מטר | 40–80 מטר | 15–40 מטר |
| **Lancet** | 150–250 מטר | 70–130 מטר | 25–60 מטר |

> **הפיזיקה לא משתנה:** עוצמת קול יורדת עם ריבוע המרחק.
> מרחק × 2 → SNR יורד פי 4.
> `SNR ∝ 1 / r²`
> מיקרופון טוב יותר מרחיב טווח פי 1.5–2, אבל לא יותר מזה.

---

### בעיית Multi-Drone — למה המערכת רואה רק פיק אחד

הגלאי לוקח את הצומת **הגבוה ביותר** ב-FFT. אם 3 FPV עפים יחד:

```
FFT שמע:
  ├── FPV מס' 1: f₀ = 847 Hz, עוצמה = 9,200   ← הזוכה — הכי קרוב
  ├── FPV מס' 2: f₀ = 831 Hz, עוצמה = 4,100   ← נטמן תחת מס' 1
  └── FPV מס' 3: f₀ = 912 Hz, עוצמה = 2,300   ← נטמן

→ המערכת מדווחת: f₀ = 847 Hz, Type = FPV 5"
→ שני הרחפנים הנוספים "נעלמים"
```

**השלכה:** בהתקפת נחיל, המערכת תזהה **אחד בלבד** — הקרוב ביותר.  
**פתרון עתידי:** Multi-peak detection — ניתוח 3–5 פיקים במקביל.

---

### Fiber-Optic FPV — מדוע הזמן קצר

FPV רגיל עם ח-בר רדיו RF → לאות בקר יש השהייה ≈ 30–80ms → ניתן לזהות + לגבש תגובה.

FPV מוכוון fiber-optic → השהייה < 1ms → הרחפן כבר מגיב לפני שהאות מגיע.

**אין הבדל אקוסטי:** f₀ זהה לחלוטין. המערכת מזהה fiber-optic כמו כל FPV 5".  
**ההבדל:** זמן תגובה מזיהוי לפגיעה ≈ 2–4 שניות. נדרשת תגובה ידנית מהירה מאוד.

---

### אין זיהוי כוונה

```
DJI Mavic של עיתונאי    →  f₀ = 183 Hz, Type = DJI Mavic (Recon)
DJI Mavic של ספוטר אש   →  f₀ = 183 Hz, Type = DJI Mavic (Recon)
```

המערכת מזהה **נוכחות** — לא **מטרה**. ההחלטה מה לעשות נשארת אצל האדם.

---

### הגבלות שרת HTTP

- `http://localhost:8765` — **אין HTTPS**, **אין אימות**
- אל תנגישו את הפורט לרשת ציבורית
- לגישה מרחוק בטוחה: SSH tunnel בלבד
  ```bash
  ssh -L 8765:localhost:8765 user@field-laptop
  # ואז בדפדפן: http://localhost:8765
  ```

---

### סיכום — מה המערכת כן יכולה

```
✓ זיהוי FPV מסחרי רגיל מ-50 עד 300 מטר
✓ הבחנה בין 7 סוגי רחפנים לפי BPF
✓ זיהוי כיוון (מתקרב/מתרחק) בדיוק ±2 שניות
✓ שיחרור מיד + זיהוי Shahed-136 (SNR נמוך מספיק)
✓ שילוב ויזואלי + אקוסטי לביטחון גבוה (CONFIRMED)
✓ הקלטת CSV לניתוח בדיעבד
✓ קליברציה אוטומטית לסביבה ספציפית
```

```
✗ זיהוי מעל 350 מטר (פיזיקה)
✗ יותר מרחפן אחד בו-זמנית
✗ הבחנה בין כוונות (עיתונאי vs. מתקפה)
✗ FPV שקט מאוד מתחת ל-50 מטר ברעש חזק
✗ זיהוי בזמן אמת של נחיל (> 3 רחפנים)
```

---

*מסמך זה נכתב עבור drone_detector.py v3 | כל הזכויות שמורות*
